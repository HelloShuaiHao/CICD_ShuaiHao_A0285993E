name: My closest CI project to real world development
on:
    push:
        branches:
            - v\d\.\d
jobs:
    myCI:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout vulnerability
          uses: actions/checkout@master
        